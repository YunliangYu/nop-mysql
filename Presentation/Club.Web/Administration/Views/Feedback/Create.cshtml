@model FeedbackModel
@{
    //page title
    ViewBag.Title = T("Admin.Feedbacks.Feedback.AddNew").Text;
    Html.SetActiveMenuItemSystemName("Feedback");
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "feedback-form" }))
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Feedbacks.Feedback.AddNew")
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Admin.Feedbacks.Feedback.BackToList").Text, "List")</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
  
}