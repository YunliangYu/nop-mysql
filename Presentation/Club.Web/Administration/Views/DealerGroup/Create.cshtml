@model DealerGroupModel
@{
    //page title
    ViewBag.Title = T("Admin.Dealers.DealerGroups.AddNew").Text;
    Html.SetActiveMenuItemSystemName("Dealer Group");
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "dealergroup-form" }))
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Dealers.DealerGroups.AddNew")
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Admin.Dealers.DealerGroups.BackToList").Text, "List")</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)

}